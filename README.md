# Utilisation du projet avec Docker

Pour build une première fois le projet, puis le lancer
```bash
docker compose up --build
```

Pour juste lancer le projet déjà build
```bash
docker compose up
```

ou 

```bash
docker compose up -d
```

Down les volumes et supprimer 
```bash
docker compose down -v
```